---
title: Difficulty-Gated Fusion of Reasoning Views for Temporal Retrieval
title_zh: 难度门控的推理视图融合用于时间检索
authors: "Jamie Holdcroft, Abdelrahman Abdallah, Adam Jatowt"
date: 2026-08-09
pdf: "https://arxiv.org/pdf/2608.08940v1"
tags: ["query:brb-er"]
score: 7.0
evidence: 基于难度门控的推理视图融合与权重估计，类似于证据推理的加权合成
tldr: "推理密集的时间检索需要匹配共享时间推理的查询和文档，现有方法通过查询重写生成多个显式时间意图并等权融合各视图排名。我们提出查询难度门控融合，从每个视图提取八维评分分布特征，如softmax熵、分数差距与离散度，用约千参数的门控网络为每个查询学习视图权重。在Tempo基准上，该方法一致提升六种检索器，最强nDCG@10达到0.297和0.303，且推理时不需标签、不重排序、不微调。轻量门控尤其强化弱骨干，但相比逐查询oracle的0.364仍有提升空间，下一步可聚焦逐查询视图选择。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.08940v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1284, \"height\": 714, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.08940v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1483, \"height\": 574, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08940v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 720, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.08940v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 683, \"height\": 168, \"label\": \"Table\"}]"
motivation: 现有查询扩展后等权融合多个推理视图，但不同视图的可靠性随查询变化，固定权重会损失精度。
method: 从每个视图提取评分分布熵、分数差距等八维特征，由约千参数的门控网络映射为逐查询权重，再加权融合排序。
result: "在Tempo基准上，该方法一致提升六种检索器，最强nDCG@10达到0.297和0.303，且无需标签、重排序或微调。"
conclusion: 轻量门控融合显著提升弱骨干，但距逐查询oracle的0.364仍有差距，下一步可聚焦逐查询视图选择。
---

## 摘要
推理密集型的时间检索要求将查询与文档匹配，其相关性取决于共享的时间推理，而非词汇重叠。将查询扩展为几种使其时间意图明确的改写形式，并用每种形式进行检索，可提供这种推理，但以等权重融合产生的排序会损失准确性：对于任何单个查询，只有部分改写形式是可靠的。我们提出了查询难度门控的推理视图融合方法。从每个视图中，我们读取其分数分布的八维特征，该特征由查询性能预测量（如softmax熵、分数差距和离散度）构建，一个约一千个参数的门控将这些特征映射为每个查询的视图权重。融合排序在推理时不使用相关性标签，不进行重排序，也不对检索器进行微调；门控采用留一任务方式训练。在\textsc{Tempo}基准上，该方法提升了我们评估的全部六个检索器（从BERT编码器到7B解码器检索器），在较弱的骨干模型上提升最大。最强的检索器达到了$0.297$和$0.303$的nDCG@10，在配对自助法检验（$p<0.001$）下，相比原始查询的每查询增益显著。每查询的oracle达到了$0.364$，而我们实际为$0.297$，这揭示了改进空间，并将每查询视图选择确定为具体的下一步方向。

## Abstract
Reasoning-intensive temporal retrieval requires matching a query to documents whose relevance depends on shared temporal reasoning rather than lexical overlap. Expanding a query into several reformulations that make its temporal intent explicit, and retrieving with each, supplies this reasoning, but fusing the resulting rankings with equal weights wastes accuracy: for any single query, only some reformulations are reliable. We propose query-difficulty-gated fusion of reasoning views. From each view we read an eight-dimensional signature of its score distribution, built from query-performance-prediction quantities such as softmax entropy, score gaps, and dispersion, and a gate of roughly one thousand parameters maps these signatures to per-query view weights. The fused ranking uses no relevance labels at inference, no re-ranking, and no fine-tuning of the retriever; the gate is trained leave-one-task-out. On the \textsc{Tempo} benchmark, the method improves all six retrievers we evaluate, from BERT encoders to 7B decoder retrievers, with the largest gains on the weaker backbones. The strongest retrievers reach $0.297$ and $0.303$ nDCG@10, and the per-query gain over the original query is significant under a paired bootstrap ($p<0.001$). A per-query oracle reaches $0.364$ against our realized $0.297$, exposing headroom that identifies per-query view selection as a concrete next step.